/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { CheckStatus } from './checkStatus';

export interface RouteTableCheck {
  /** Routing table ID. */
  table_id: number;
  /** Outbound tag associated with this table. */
  outbound_tag: string;
  /** Expected route destination. Present when known. */
  expected_destination?: string;
  /** Expected egress interface. Present when configured. */
  expected_interface?: string;
  /** Expected gateway IP. Present when configured. */
  expected_gateway?: string;
  /** Expected route metric/priority. Present when non-zero. */
  expected_metric?: number;
  /** Expected route type. */
  expected_route_type?: string;
  /** Whether the routing table exists in the kernel. */
  table_exists: boolean;
  /** Whether a default route (0.0.0.0/0 or ::/0) is present in the table. */
  default_route_present: boolean;
  /** Whether the actual egress interface matches the expected one. */
  interface_matches: boolean;
  /** Whether the actual gateway matches the expected one. */
  gateway_matches: boolean;
  status: CheckStatus;
  /** Human-readable detail. Omitted when empty. */
  detail?: string;
}
